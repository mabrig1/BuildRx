/**
 * Long product specifications are normal for app generation. Twelve
 * thousand characters is roughly 2,500-3,500 tokens: enough for roles,
 * workflows, data models, and acceptance criteria without crowding the
 * model's output budget.
 */
export const MAX_AI_PROMPT_CHARACTERS = 12_000;
